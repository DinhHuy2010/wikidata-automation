{
    "name": "UV Python Development",
    "build": {
        "dockerfile": "Dockerfile"
    },
    "remoteUser": "huyonunix",
    "postCreateCommand": "uv sync",
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python"
            ],
            "settings": {
                "python.defaultInterpreterPath": ".venv/bin/python"
            }
        }
    }
}
